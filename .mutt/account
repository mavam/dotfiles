# Personal constants
set realname="Matthias Vallentin"
alternates '(matthias@vallentin.net|vallentin?@(icsi.berkeley.edu|icir.org)|matthias.vallentin@gmail.com|(vallentin|mavam|mpm)@berkeley.edu|(mavam|vallentin|matthias.vallentin)@(ee)?cs.berkeley.edu|matt?(hias)?@bro.org)'

unmy_hdr *
my_hdr X-PGP-Fingerprint: 8A3B 1323 B469 CCBA 54D3  3BCC D5E7 8DF5 9C8D 4B41
set pgp_sign_as="0x9C8D4B41"

#
# Local setup via offlineimap.
#
#set mbox_type=Maildir
#set folder=~/.mail
#set spoolfile==INBOX
#set postponed==drafts

#
# GMail via IMAPS.
#
set imap_check_subscribed=yes # automatically retrieve labels
set imap_user = 'matthias@vallentin.net'
set spoolfile = imaps://imap.gmail.com/INBOX
set folder = imaps://imap.gmail.com/
set postponed="imaps://imap.gmail.com/[Gmail]/Drafts"
set message_cachedir = ~/.mutt/cache_body
set header_cache = ~/.mutt/cache_header

set smtp_url="smtps://matthias@vallentin.net@smtp.gmail.com:465/"
set smtp_authenticators='gssapi:login'
unset record # Because Google's SMTP server already saves a copy.
#set record="imaps://imap.gmail.com/[Gmail]/Sent Mail"

# Default profile.
source ~/.mutt/profiles/icir

# vim: ft=muttrc
