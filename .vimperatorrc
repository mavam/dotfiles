set editor=mvim

" Add bookmarks toolbar.
set guioptions+=B

" Completion priority
" bookmarks, awesome bar, suggestions
set complete=blS

" Show completions automatically
set wildoptions=auto

" Always open pages in a new tab
set newtab=all

" vim: ft=vimperator
