# 
# MacOS (Darwin) resource files.
#

# MacVIM alias.
if [[ -d /Applications/MacVim.app ]]; then
    alias gvim='/Applications/MacVim.app/Contents/MacOS/Vim -g'
fi
which mvim &> /dev/null && alias m='mvim'

# Unload the TUN Kernel extension (to make OpenVPN work).
unload_tun()
{
    sudo kextunload /System/Library/Extensions/tun.kext
}

# vim: ft=zsh
